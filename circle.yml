machine:
  node:
    version: 0.12.0
dependencies:
  override:
    - npm install --production
    - npm install -g bower gulp
    - bower install
deployment:
  dev:
    branch: circleci
    commands:
      - gulp build
      - ls -la
      - rsync -e "ssh -p $SSH_PORT" dist/* $FRONTEND_USER@tf2stadium.com:$FRONTEND_DEPLOY_PATH
      - ssh -p $SSH_PORT $FRONTEND_USER@tf2stadium.com $FRONTEND_DEPLOY_SCRIPT
